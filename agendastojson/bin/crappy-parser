#!/usr/bin/perl

@lines = `wget -O - GET http://www.cityofomaha.org/cityclerk/city-council/agendas`;

mkdir data;
for(@lines) {
    if (/.pdf/ && /agenda/ && m/href="([^"]+)"/) {
        print $1."\n";
        `wget -P data http://www.cityofomaha.org$1 `;
    }
}
